services:
  tuner:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Data Source=/data/drumtuner.db
    volumes:
      - db-data:/data

volumes:
  db-data:
